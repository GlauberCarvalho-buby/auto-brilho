// Este arquivo não é mais usado - a página 404 foi movida para NaoEncontrada.tsx
// Mantido aqui apenas para compatibilidade com o template

import NaoEncontrada from "./NaoEncontrada";

export default function NotFound() {
  return <NaoEncontrada />;
}
